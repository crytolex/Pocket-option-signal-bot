services:
  - type: web
    name: pocket-option-signal-bot
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: $TELEGRAM_BOT_TOKEN
      - key: ADMIN_CHAT_IDS
        value: $ADMIN_CHAT_IDS
      # Add other secrets here if needed
